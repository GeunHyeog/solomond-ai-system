# SOLOMOND AI - pip 안정 설치용 패키지들
# 복잡하고 바이너리 의존성이 있는 AI 패키지들

# 핵심 AI 라이브러리 (CUDA/바이너리 의존성)
torch>=2.0.0
transformers>=4.30.0
sentence-transformers>=2.2.0

# 음성 처리 (복잡한 의존성)
openai-whisper>=20231117
openai>=1.3.0
anthropic>=0.8.0

# 오디오 처리 (네이티브 라이브러리)
soundfile>=0.12.1
librosa>=0.10.1
scipy>=1.11.0

# 컴퓨터 비전 (복잡한 빌드)
easyocr>=1.7.0

# 화자 분리 (특수 의존성)
pyannote.audio>=3.1.0
speechbrain>=0.5.0

# 최근 추가된 필수 패키지들
ollama>=0.5.0
schedule>=1.2.0